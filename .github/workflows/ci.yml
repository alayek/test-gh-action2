name: CI Pipeline

on:
  pull_request:
    branches:
      - main

jobs:
  ci_build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Commit 🛒
        uses: actions/checkout@v4

      - name: Print Something
        run: echo "No ci file in main"
